using System;

namespace HelloWorld
{
  class Program
  {
    static void Main(string[] args)
    {
      //  TODO: Get a valid input 
          
          do
          {
            
          
          
          //   TODO: Provide the user a menu of options

          Console.WriteLine("What's your pleasure?");
          Console.WriteLine("L: Load the data file into an array.");
          Console.WriteLine("S: Save the array to the data file");
          Console.WriteLine("C: Add a name to the array.");
          Console.WriteLine("R: Read a name from the array.");
          Console.WriteLine("U: Update a name from the array.");
          Console.WriteLine("D: Update a name from the array.");
          Console.WriteLine("Q: Quit the program.");
          
          

          //   TODO: Get a valid user option (valid means its on the menu)
          string userChoice = Console.ReadLine();

          bool userChoice = (userChoiceString=="A") || userChoiceString=="a");

          }

      //   TODO: If the option is is L or l then load the text file into the array 

      //   TODO: Else if the option is an S or s then store the array of strings into the text file

      //   TODO: Else if the option is a C or c then add a name to the array (if there's room)

      //   TODO: Else if the option is an R or r then read a name from the array (if it's there)
                    ///message "error, didn't find that data" 

      //   TODO: Else if the option is a U or u then update a name in the array (if it's there)

      //   TODO: Elese if the option is a D or d then delete the name in the array (if it's there)

      //   TODO: Else if the option is a Q or q then quit the program 

    }      
    
  }
}